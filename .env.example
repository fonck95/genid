# GenID Environment Variables
# Copy this file to .env and fill in your values

# =============================================================================
# VERTEX AI CONFIGURATION
# =============================================================================

# Google Cloud Project ID (REQUIRED)
# Can be one of:
# - Direct project ID: my-project-123
# - Service account email: name@my-project-123.iam.gserviceaccount.com
#
# Priority order: VITE_APP_ID_VERTEX > VITE_APP_ID > VITE_VERTEX_PROJECT_ID
VITE_APP_ID_VERTEX=your-project-id

# Vertex AI Region (optional, default: us-central1)
# Supported regions for Veo: us-central1, us-east1, us-west1,
#                            europe-west1, europe-west4,
#                            asia-northeast1, asia-southeast1
VITE_VERTEX_LOCATION=us-central1

# Vertex AI Access Token (OPTIONAL)
# Pre-generated access token for Vertex AI API
# This is used as a fallback when OAuth authentication is not available
# Generate with: gcloud auth print-access-token
# Note: Access tokens expire after 1 hour - use OAuth for production
# VITE_APP_API_KEY_VERTEX=ya29.xxx...

# =============================================================================
# GOOGLE OAUTH2 CONFIGURATION
# =============================================================================

# OAuth2 Client ID (REQUIRED)
# Create at: https://console.cloud.google.com/apis/credentials
VITE_APP_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# OAuth2 Client Secret (REQUIRED)
VITE_APP_GOOGLE_CLIENT_SECRET=your-client-secret

# OAuth Callback URL (optional)
# Defaults to {window.location.origin}/auth/callback
# VITE_APP_GOOGLE_CALLBACK_URL=http://localhost:5173/auth/callback

# =============================================================================
# GEMINI API CONFIGURATION
# =============================================================================

# Gemini API Key for face analysis (optional but recommended)
# Get at: https://aistudio.google.com/app/apikey
VITE_APP_API_KEY_GOOGLE=your-gemini-api-key

# =============================================================================
# KLING AI VIDEO GENERATION (SERVER-SIDE)
# =============================================================================

# Kling AI API credentials for video generation
# Get at: https://www.klingai.com/
# Note: These are SERVER-SIDE variables used by Vercel serverless functions
KLING_ACCESS_KEY=your-kling-access-key
KLING_SECRET_KEY=your-kling-secret-key

# Kling Video Generation System Prompt (optional - has sensible defaults)
# This prompt is used to enhance video generation quality and maintain facial consistency
# Placeholders: {FACE_DESCRIPTION} = anthropometric facial data, {USER_PROMPT} = user movement request
KLING_VIDEO_SYSTEM_PROMPT="CINEMATIC VIDEO GENERATION - PHOTOREALISTIC HUMAN MOTION. IDENTITY PRESERVATION (CRITICAL): Maintain absolute facial consistency with the anthropometric profile. Every frame must preserve exact facial bone structure, orbital region details, nasal anatomy, labial features, skin characteristics, and hair patterns. PHOTOREALISTIC MOTION: Natural physics with weight distribution and momentum. Micro-expressions with subtle facial muscles, natural blinks every 3-5 seconds. Visible breathing rhythm. Natural skin dynamics with subsurface scattering. Minor imperfections and asymmetries. CINEMATOGRAPHY: Consistent lighting (no AI flickering), natural motion blur, depth of field coherence, color grading continuity, realistic shadows. AVOID: Plastic skin, morphing artifacts, unnatural eye movements, robotic motion, temporal inconsistency, over-smoothed texture, physics-defying elements. OUTPUT: Indistinguishable from professional 4K cinema footage. [FACE_ANTHROPOMETRY] {FACE_DESCRIPTION} [/FACE_ANTHROPOMETRY] [MOTION] {USER_PROMPT} [/MOTION]"

# =============================================================================
# SETUP CHECKLIST
# =============================================================================
# Before running the app, ensure:
#
# 1. Vertex AI API is enabled:
#    https://console.cloud.google.com/apis/library/aiplatform.googleapis.com
#
# 2. OAuth consent screen is configured:
#    https://console.cloud.google.com/apis/credentials/consent
#
# 3. OAuth credentials are created with correct redirect URIs:
#    - http://localhost:5173/auth/callback (development)
#    - https://your-domain.com/auth/callback (production)
#
# 4. Users have Vertex AI IAM permissions:
#    - roles/aiplatform.user (minimum)
#    - roles/aiplatform.admin (full access)
